/**
 * Estampille de compilation, injectée par Vite (voir `vite.config.ts`).
 * Affichée dans le panneau des réglages : sans elle, rien ne distingue
 * « le correctif ne marche pas » de « le navigateur sert l'ancienne page ».
 */
declare const __BUILD_STAMP__: string;
